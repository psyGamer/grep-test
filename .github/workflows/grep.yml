on:
  push:
    branches: 
      - '*'

jobs:
  grep:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - run: ls -la
      - run: grep --version
      - run: grep -Po '(?<=<Version>)\S+(?=<\\/Version>)' CelesteStudio.csproj
